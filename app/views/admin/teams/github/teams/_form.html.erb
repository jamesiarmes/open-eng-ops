<%= form_with(model: @relation, id: 'team-github-team', url: admin_team_github_teams_url(team_id: @relation.team_id, id: 1), data: { controller: 'form' }, class: 'contents') do |form| %>
  <%= form.hidden_field :team_id %>

  <%= render 'shared/form/select', form: form,
             field: :service_id,
             options: options_for_select(
               Service.all.map { |s| [s.name, s.id]},
               @relation.service&.id
             ),
             required: true %>

<!--  TODO: Build an interface that uses the GitHub API to select a team (teams?).-->
  <%= render 'shared/form/text_field', form: form,
      field: :github_team_id, required: true %>

<!--  <div class="modal-footer">-->
    <%#= form.submit value: action_name == 'new' ? t('.create') : t('.update'), class: 'btn btn-primary' %>
    <%#= link_to 'Cancel', admin_team_path(@relation.team_id), class: 'button-cancel', data: { action: 'turbo-modal#hideModal' } %>
<!--  </div>-->
<% end %>
