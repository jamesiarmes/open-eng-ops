<%= render Modal::Component.new title: t('devise.invitations.new.header') do |modal| %>
  <% modal.with_icon icon: 'user-plus', classes: 'default-avatar-xs me-1' %>
  <%= render 'form' %>
  <% modal.with_footer do %>
    <%= submit_tag t('devise.invitations.new.submit_button'), class: 'btn btn-primary', form: 'user-invitation' %>
  <% end %>
<% end %>
